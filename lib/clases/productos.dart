class productos {
  final id;
  final nombre;
  final autor;
  final imagen;
  int cantidad;
  int precio;

  productos(this.id, this.nombre, this.autor, this.imagen, this.cantidad,
      this.precio);
}
